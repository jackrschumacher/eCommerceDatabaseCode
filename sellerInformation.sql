CREATE TABLE sellerDetails(
	sellerID int PRIMARY KEY,
	sellerName varchar(MAX),
	sellerDescription varchar(MAX),
	sellerContactPhone int,
	sellerContactEmail varchar(MAX),
	sellerAccountActiveTime tinyint,
);
