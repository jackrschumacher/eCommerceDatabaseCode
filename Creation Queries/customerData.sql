CREATE TABLE customerData(
	customerID INT,
	customerFirstName Varchar(MAX),
	 customerLastName Varchar(MAX),
	customerPhoneNumber TINYINT ,
	customerEmail Varchar(MAX) ,
	lastAccountActivityDate date,
	accountType varchar(MAX),
	purchaseMethod varchar(MAX),
	cardDetails tinyint 
);
